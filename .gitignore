# Confidential source material — deliberately NOT published to the public repo.
# The site is public; the raw pitch, strategy notes and internal positioning are not.
# Remove a line (or use `git add -f`) if you want something tracked.
docs/
CLAUDE.md
archive/papers/
archive/archive/
archive/PITCH.md

node_modules/
.DS_Store
